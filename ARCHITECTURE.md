# 架构与分层规划（初步）

- 目标
  以现有的 meilisearch_ui 为起点，进行分层、模块化重构的初步设计，降低耦合、提升可维护性与测试性。

- 模块划分（初步版）
  - index_panel：处理索引列表展示、当前索引选择、索引元数据获取等与索引相关的 UI 入口点。
  - query_editor：负责构造和管理查询条件的 UI 组件，以及对应的数据结构。
  - results_table：展示搜索结果表格、字段编辑、排序、分页等交互。
  - view_config：负责视图/列配置、宽度、标签等元信息的编辑和持久化。
  - ai_config：AI 相关开关、权重等配置的 UI 与持久化。

- 数据流与边界
  - App 作为应用的总入口，持有全局状态与流程控制，其他模块作为独立的功能入口点暴露简单接口。
  - 各模块暴露的只是数据结构和最小必要的方法，避免直接引用彼此的实现细节。
  - HTTP/网络交互通过独立的服务层（在未来实现）来封装，与 UI 组件解耦。

- 代码组织（未来演进）
  - 将现有的大型 src/main.rs 按功能拆分为上述模块，并通过 mod 进行组织。
  - 每个模块维护自己的数据模型和最小行为集合，避免跨模块的全局状态耦合。
  - 引入简单的测试分层：单元测试、集成测试、端到端测试的初步框架。

- 测试策略（分层）
  1) 单元测试
     - 针对数据模型、序列化/反序列化、以及小型工具函数。
     - 未来使用 cargo test 对新模块进行测试。
  2) 集成测试
     - 模拟 API 调用，验证模块之间的数据协作与接口契合。
     - 使用 mock 服务器或内置的 mock 实现来替代真实网络请求。
  3) WASM/前端测试
     - 初期可引入 wasm-bindgen-test 进行浏览器环境下的简单测试，逐步覆盖核心 UI 逻辑。

- 里程碑（初步）
  - M1：完成模块化骨架（index_panel, query_editor, results_table, view_config, ai_config）及 main.rs 的最小集成点。
  - M2：实现最小可用的服务层接口，完成一次简单的“加载索引 + 执行一次搜索”的端到端核验。
  - M3：引入简单的测试用例集合，覆盖关键数据结构。

—— 结束
